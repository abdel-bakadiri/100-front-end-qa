const [day, mount, year] = ["10", "09", "2020"];
console.log(`${day}/${mount}/${year}`); // 10/09/2020
let { firstName: fn, cuntry, city, score } = {
  firstName: "Abdel",
  cuntry: "France",
  city: "Paris",
  score: 100,
};
console.log(`${fn} lives in ${city}`);
